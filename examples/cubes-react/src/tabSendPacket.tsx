
import * as React from 'react'
import { unionBy } from 'lodash'
import cx from 'classnames'

import { Cube } from '../../..'

export interface TabSendPacketProps {
    cube: Cube
}

export const TabSendPacket = ({ cube }: TabSendPacketProps) => {

    return (
        <div>
            TabSendPacket
        </div>
    )
}
